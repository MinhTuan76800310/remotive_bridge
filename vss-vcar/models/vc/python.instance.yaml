schema: remotive-topology-instance:0.17

platform:
  includes:
    - ../../platform/vss-hmi-vcar.platform.yaml

ecus:
  VC:
    models:
      vc:
        type: container
        container:
          build:
            args:
              - MODEL_PATH=vc/python
            target: model
            dockerfile: ../../Dockerfile
          command: python -m vc
